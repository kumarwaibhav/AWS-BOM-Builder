/**
 * Client-side session id: a random id generated once per browser and kept in
 * localStorage, used to scope bill history. No login, no accounts.
 */
export const SESSION_STORAGE_KEY = "aws-bom-sessionId";
