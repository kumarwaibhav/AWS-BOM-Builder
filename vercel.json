{
  "buildCommand": "vite build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install",
  "framework": null,
  "outputDirectory": "dist/public",
  "env": {
    "DATABASE_URL": {
      "description": "MySQL/TiDB connection string",
      "required": true
    },
    "AWS_REGION": {
      "description": "AWS region for the S3 bucket, e.g. us-east-1",
      "required": true
    },
    "AWS_ACCESS_KEY_ID": {
      "description": "IAM access key scoped to the S3 bucket",
      "required": true
    },
    "AWS_SECRET_ACCESS_KEY": {
      "description": "IAM secret key scoped to the S3 bucket",
      "required": true
    },
    "AWS_S3_BUCKET": {
      "description": "S3 bucket name for storing uploaded PDFs and generated Excel BOMs",
      "required": true
    },
    "GEMINI_API_KEY": {
      "description": "Optional: enables AI enrichment of ambiguous line items (free tier at aistudio.google.com/apikey)",
      "required": false
    }
  },
  "functions": {
    "api/index.ts": {
      "memory": 512,
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "no-cache, no-store, must-revalidate"
        }
      ]
    }
  ]
}
